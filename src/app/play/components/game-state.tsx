"use client";
import { GameState } from "@/lib/types";
import { useSearchParams } from "next/navigation";
const GameSession = ({ productId }: GameState) => {
  const searchParams = useSearchParams();
};
